```go
package main

import (
  "fmt"
  "strings"
)

func main() {
  var a int
  fmt.Scanln(&a)
  for i:=1; i < a+1; i++{
    fmt.Println(strings.Repeat("*",i))
  }
}
```

```py
n = int(input())

for i in range(1, n + 1):
  print('*' * i)
```

```sh
read N
for((i=1;i<=N;i++)); do
  for((j=1;j<=$i;j++)); do
    printf "*"
  done
  echo
done
```
